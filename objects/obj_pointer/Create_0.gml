event_inherited();

// Pointer visual settings
image_index = 0;  // Always use frame 0 (solid black arrow)
image_speed = 0;  // Don't animate
image_alpha = 0;  // Hidden by default (save/load menu will show selected pointer)