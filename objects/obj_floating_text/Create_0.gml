// Floating text properties
text = "Text";
text_color = c_white;
float_speed = 0.75;
fade_speed = 0.03;
lifetime = 40; // Frames until it starts fading

// Damage type (for colored damage numbers)
damage_type = DamageType.physical;

// Animation
alpha = 1;
y_offset = 0;

// Parent character to follow
parent_instance = noone;
offset_x = 0;
offset_y = 0;

// Font setup
draw_font = -1;
text_scale = 0.25; // Make text smaller (50% of original size)
